#!/bin/bash
source `dirname $0`/config.sh

if [ -z $1 ]; then
    java -jar $LEVI_PATH github-list-repos --user $GITHUB_USER --github-url $GITHUB_URL --org $GITHUB_ORG
else
    java -jar $LEVI_PATH github-list-repos --user $GITHUB_USER --github-url $GITHUB_URL --org $1
fi
